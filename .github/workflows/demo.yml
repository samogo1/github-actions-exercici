name: GitHub Actions Demo

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  prova:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Missatge
        run: echo "Hola des de GitHub Actions"

      - name: Mostrar fitxers
        run: ls -la